services:
  openrouter-dashboard:
    build: .
    image: openrouter-dashboard:latest
    container_name: openrouter-dashboard
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - TZ=UTC
    restart: unless-stopped
